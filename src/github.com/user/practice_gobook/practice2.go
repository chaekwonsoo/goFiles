
package main
/*
import (
	"fmt"
)

func main() {
	ch := make(chan int)
	go send(ch)
	go receive(ch)
	
	var input string
	fmt.Scanln(&input)
	fmt.Println(input)
}

func send(sch chan int) {
	for i := 0; i < 100; i++ {
		sch <- i
	}
}

func receive(sch chan int) {
	for i := 0; i < 100; i++ {
		fmt.Println(<-sch)
	}
}
*/
