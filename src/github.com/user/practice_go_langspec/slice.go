package main
/*

import "fmt"

func main() {
	slice1 := make([]int, 50, 100)
	slice2 := new([100]int)[0:50]

	for i := 0; i < 3; i++ {
		slice1[i] = i
		slice2[i] = i*2
	}

	for i := 0; i < 3; i++ {
		fmt.Print(slice1[i], slice2[i])
		fmt.Println("")
	}

	fmt.Println(" --- ")
	fmt.Println(slice1[5])
	fmt.Println(slice1[20])
	//fmt.Println(slice1[70])
	fmt.Println(" --- ")

	slice3 := slice1[0:100]

	for i := 0; i < 100; i++ {
		slice3[i] = i
	}

	for i := 0; i < 100; i++ {
		fmt.Println(slice3[i])
	}
}
*/
