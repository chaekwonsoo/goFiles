package main

import (
	//"fmt"
	//"runtime"
)

/*
func foo(s string, n int) {
	for i := 0; i < n; i++ {
		runtime.Gosched()
		fmt.Println(s, i)
	}
}

func goo(s string, n int) {
	for i := 0; i < n; i++ {
		fmt.Println(s, i)
	}
}
*/

func main() {
	
	//go goo("GOROUTINE", 100)
	//foo("FUNCTION", 10)
	//fmt.Println(runtime.NumCPU())
	
}
